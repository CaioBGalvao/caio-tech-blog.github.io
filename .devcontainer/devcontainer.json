{
    "name": "Existing Docker Compose (Extend)",
    "dockerComposeFile": ["../docker-compose.yml"],
    "service": "blog",
    "workspaceFolder": "/var/www/html",
    "customizations": {
        "vscode": {
            "extensions": []
        }
    }
}